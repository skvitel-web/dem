spring.application.name=river-driver-training

# PostgreSQL — база driver_rf (pgAdmin / DBeaver, схема portal_*)
spring.datasource.url=jdbc:postgresql://localhost:5432/driver_rf
spring.datasource.username=postgres
spring.datasource.password=Dragon!55
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Не пересоздаём таблицы — БД уже есть
spring.sql.init.mode=never

server.port=8045

# Локальные логин/пароль БД: скопируйте application-local.properties.example → application-local.properties
